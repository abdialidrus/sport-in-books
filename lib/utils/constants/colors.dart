import 'package:flutter/material.dart';

class TColors {
  /// App theme colors
  static const Color primary = Color(0xFF01C4D7);

  // Text colors
  static const Color textPrimary = Color(0xFF01C4D7);
  static const Color textSecondary = Color(0xFF002830);

  // Background colors
  static const Color primaryBackground = Colors.white;

  // Neutral shades
  static const Color white = Color(0xFFFFFFFF);
}
